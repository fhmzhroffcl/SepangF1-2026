import ReactMarkdown from 'react-markdown';
import React,{useEffect,useState} from 'react';
export default function AIBriefing({date='',topic='weather',auto=false}){
 const [state,setState]=useState({}),[attempt,setAttempt]=useState(auto?1:0);
 useEffect(()=>{if(!attempt)return;const controller=new AbortController();setState({loading:true});fetch(`/api/briefing?topic=${topic}&date=${date}`,{signal:controller.signal}).then(async r=>{const j=await r.json();if(!r.ok)throw Error(j.error||'Briefing unavailable');return j;}).then(j=>setState(j)).catch(e=>{if(e.name!=='AbortError')setState({error:e.message});});return()=>controller.abort();},[date,topic,attempt]);
 return <div className="ai-briefing"><strong>{topic==='weather'?'Weather, in plain English':'Your circuit engineer'}</strong>{state.loading?<p role="status">{topic==='weather'?'Checking Sepang’s forecast…':'Explaining the circuit and what to watch…'}</p>:state.text?<><div className="briefing-answer"><ReactMarkdown>{state.text}</ReactMarkdown></div><small>{topic==='weather'?`AI weather summary · ${state.date} · ${state.source}`:'AI circuit explanation · official circuit facts'}</small></>:state.error?<p role="status">{state.error}</p>:<p>{topic==='weather'?'What the forecast means for your day.':'Understand the lap, the corners and the racing.'}</p>}<button disabled={state.loading} onClick={()=>setAttempt(n=>n+1)}>{state.text?'Refresh briefing':state.error?'Retry briefing':'Get briefing'}</button></div>;
}
